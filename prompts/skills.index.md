## Learned Procedural Skills (mandatory)
> `memex-` is an internal storage prefix — these skills were **saved from YOUR past experience** and encode proven workflows, pitfalls, and preferred approaches for specific task types. They are NOT about the memex plugin.

Before replying, scan the list. If **any** skill is even partially relevant to the current task, you **MUST** load it immediately with `skills_tool method=load skill_name=NAME` and follow its instructions. Err on the side of loading — missing a relevant skill is always worse than loading one you don't need.

<available_learned_skills>
{{index}}
</available_learned_skills>
